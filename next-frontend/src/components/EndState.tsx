import * as React from 'react';
import {
    Box,
} from '@chakra-ui/react';

const EndState: React.FC = () => {
    
    return <Box>
        Game over, winner: 
    </Box>
};

export default EndState;
